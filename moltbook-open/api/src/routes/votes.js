/**
 * Vote routes
 * Placeholder for potential future vote-specific endpoints
 * Main voting is handled in posts.js and comments.js
 */

const express = require('express');
const router = express.Router();

// Currently voting is handled in post and comment routes
// This file exists for potential future expansion

module.exports = router;
